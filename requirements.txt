# requirements.txt for the healthcare case study project
# Generated from imports in `healthcare_case_study.py` and workspace files.
# Use: python -m pip install -r requirements.txt
# Core Libraries
pandas>=1.3.5
numpy>=1.21.0
scipy>=1.7.3

# Machine Learning
scikit-learn>=1.0.2
imbalanced-learn>=0.10.1
joblib>=1.1.0

# Visualization
matplotlib>=3.4.3
seaborn>=0.11.2

# App Framework
streamlit>=1.31.0

# Notes:
# - `imbalanced-learn` provides SMOTE and imblearn.pipeline.
# - If you use a notebook (`.ipynb`), `notebook` is included. Consider adding `jupyterlab` if preferred.
# - Pin exact versions only if you need strict reproducibility; these are conservative minimums/upper bounds.
